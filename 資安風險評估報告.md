# 匹克球 Web 體感遊戲 - 雲端後端與資料庫資安風險評估報告

**專案版本**：v5.0.14  
**報告日期**：2026-09-13  
**評估對象**：前端 Web 客戶端 (`js/config.js`, `js/game.js`, `js/social.js`) 與 Google Apps Script (GAS) / Google Sheets 雲端資料庫架構  

---

## 摘要 (Executive Summary)

本遊戲目前採用**「純前端靜態架構 (Client-Side Static Web) + Google Apps Script Web App + Google Sheets 雲端資料庫」**。
在現有實作中，所有後端通訊端點（`GAS_URL`）與用以生成數位簽章的密鑰（`SIGN_SECRET`）皆直接硬編碼於前端 JavaScript 腳本中。

雖然系統具備 HMAC-SHA256 簽署與時間戳/隨機數防重放機制，但**由於密鑰暴露在客戶端，對於具備網頁開發或網路抓包技術的攻擊者而言，形同無防護**。本報告旨在盤點現行資安威脅，並提供短、中、長期的架構加固方案。

---

## 一、 現行架構弱點盤點 (Vulnerability Assessment)

```
┌─────────────────────────────────────────────────────────┐
│              客戶端瀏覽器 (任何玩家 / 潛在攻擊者)         │
│  [公開可見] GAS_URL = "https://script.google.com/..."    │
│  [公開可見] SIGN_SECRET = "nchu-pickleball-2026-secret"  │
│  [公開可見] SHA.hmac 簽章演算法                          │
└────────────────────────────┬────────────────────────────┘
                             │ 
                             │ ⚠️ 攻擊者可任意偽造合法簽章封包
                             ▼
┌─────────────────────────────────────────────────────────┐
│        Google Apps Script (GAS) 後端 (/exec 端點)        │
│  - 驗證 HMAC 簽章 (因密鑰外洩，偽造簽章亦能通過)         │
│  - 依賴前端傳入的 score、playerId、likes 進行寫入       │
└────────────────────────────┬────────────────────────────┘
                             │ 
                             ▼
┌─────────────────────────────────────────────────────────┐
│            Google Sheets 雲端資料庫 (試算表)             │
│  - 英雄榜戰績表 / 球員檔案 / 好友清單 / 數位孿生大數據  │
└─────────────────────────────────────────────────────────┘
```

### 1. [高風險] 密鑰與端點完全暴露 (Hardcoded Secret & Endpoint)
- **弱點位置**：`js/config.js` 第 15~16 行
  ```javascript
  const GAS_URL = 'https://script.google.com/macros/s/AKfycbwbQJTyJFtZjGOHs_EzYnbbQjq1Znj8KHG1l9uVhgqsyUK2KpkwdN6nydNNvyqz394mGQ/exec';
  const SIGN_SECRET = 'nchu-pickleball-2026-secret';
  ```
- **弱點說明**：任何使用者開啟瀏覽器「開發者工具 (F12) ➔ Network (網路)」或「檢視網頁原始碼」，即可一鍵複製完整的 API 網址與簽名私鑰。

---

### 2. [高風險] 戰績假造與英雄榜洗榜 (Score Spoofing & Leaderboard Poisoning)
- **攻擊場景**：
  攻擊者無須開啟遊戲遊玩，只需編寫 5 行 Python 或 Node.js 腳本，使用外洩的 `SIGN_SECRET` 與 HMAC-SHA256 算法計算 `sig`，直接向 GAS 發送 `act: 'submit'` 請求：
  ```json
  {
    "act": "submit",
    "playerId": "P-HACKER-001",
    "nickname": "外掛玩家",
    "department": "資工系",
    "score": 99999,
    "stage": 5
  }
  ```
- **影響**：後端簽章比對會判定合法，使得偽造的高分直接登上中興英雄榜首位，摧毀全體正常玩家的競技公平性。

---

### 3. [中高風險] Google 免費配額耗盡與 DoS 阻斷服務 (Quota Exhaustion Attack)
- **弱點說明**：
  Google Apps Script (GAS) 針對免費 Google 帳號有嚴格的每日配額限制：
  - **URL Fetch 呼叫次數**：每日 20,000 次。
  - **腳本同時執行數**：最多 30 個並發實例。
  - **每日總執行時間**：每日 90 分鐘。
- **影響**：攻擊者可利用高頻循環呼叫 `/exec`，在數分鐘內耗盡該 Google 帳號的當日執行額度，導致所有正常玩家在當天無法讀取排行榜、無法上傳成績（出現 `NETWORK_FAIL` 或 Google 503 超額錯誤）。

---

### 4. [中風險] 社交關係惡意操縱與女巫攻擊 (Sybil Attack & Manipulation)
- **按讚洗榜**：雖然每日限制每人對同一對象只能點讚 1 次，但由於 `playerId`（如 `P-XXXXXX-XXXX`）是由前端隨機生成，攻擊者可批次偽造數千個虛擬 `playerId` 向特定球友狂刷愛心。
- **好友騷擾**：批次向榜上全體玩家發送好友邀請，污染玩家的好友通知清單。

---

### 5. [低至中風險] 個資讀取與隱私曝露 (Data Scraping)
- **弱點說明**：
  後端未實施使用者身分授權檢驗，任何人皆可透過 `GET ?act=leaderboard` 與 `GET ?act=friends` 批次爬取全校登錄玩家的名單、學號前綴、系所年級與個人自訂暱稱。

---

## 二、 現行機制的防護效力矩陣

| 攻擊手法 | 現有機制防護能力 | 說明 |
| :--- | :---: | :--- |
| **一般玩家修改瀏覽器記憶體分數** | 🟢 **有效防禦** | 前端變數修改若未配合正確 HMAC 重新簽署，送出時會被後端擋下。 |
| **重放攻擊 (Replay Attack)** | 🟢 **有效防禦** | 封包內含 `ts`（時間戳）與 `nonce`（隨機碼），重複送出相同封包會被識別過期。 |
| **CORS 跨域阻擋繞過** | 🟡 **免 Preflight** | 採用 `text/plain` 繞過 OPTIONS 預檢，兼顧通訊但無法防止未授權調用。 |
| **外部腳本偽造簽章** | 🔴 **無法防禦** | 密鑰 `SIGN_SECRET` 在前端完全公開，任何人皆可自行產出有效簽章。 |
| **假造 playerId 刷讚** | 🔴 **無法防禦** | 未綁定實名帳號，隨機產生的 ID 即可被認定為新使用者。 |
| **配額耗盡 (DoS)** | 🔴 **無法防禦** | GAS 原生未配置 IP Rate Limit（頻率限制）與 Cloud WAF 防護。 |

---

## 三、 改善方案與演進藍圖 (Remediation Roadmap)

為在兼顧輕量化、開發成本與安全性的前提下解決上述問題，建議分階段進行加固：

```mermaid
graph LR
    subgraph 現狀 Phase 0
        A[前端網頁: 含有Secret] -->|直接呼叫| B[Google Apps Script]
    end
    subgraph 短期 Phase 1 (強烈推薦)
        C[前端網頁: 零Secret] -->|呼叫代理| D[Cloudflare Worker / Vercel]
        D -->|帶入秘密環境變數| B
    end
    subgraph 中長期 Phase 2
        E[前端網頁] -->|Google / 校園 SSO 登入| F[認證中心核發 JWT]
        F -->|附帶 JWT Token| D
        D -->|物理軌跡 Audit 驗證| G[專屬資料庫 Supabase / Sheets]
    end
```

### 【短期方案】：引入免費 Serverless 反向代理 (零成本，最快見效)
> **工具建議**：Cloudflare Workers 或 Vercel Serverless Function（均提供豐厚免費額度）

1. **隱藏真實端點與密鑰**：
   - 前端不再保存 `GAS_URL` 與 `SIGN_SECRET`。
   - 前端所有請求改發送至代理端點：`https://pickleball.your-domain.workers.dev/api/...`。
   - `GAS_URL` 與 `SIGN_SECRET` 妥善保存在 Cloudflare Workers 的 **Secrets (環境變數)** 中。
2. **內建頻率限制 (Rate Limiting)**：
   - 限制單一 IP 每分鐘最多發送 30 次請求，徹底防堵惡意爬蟲與 DoS 配額耗盡。
3. **Cloudflare DDoS 與 WAF 保護**：
   - 自動過濾常見惡意腳本、已知爬蟲與異常高頻的偽造請求。

---

### 【中期方案】：伺服器端物理合理性校驗 (Anti-Cheat Engine)
1. **成績物理軌跡驗證**：
   - 前端上傳成績時，不只傳送 `score: 15`，而是附帶該局的揮拍物理日誌摘要（`AUDIT_LOG` 摘要：每次擊球間隔、球速範圍、失誤率）。
   - 後端檢查：
     - 單局時間是否合理（例如不可能在 5 秒內打完一局）。
     - 球速是否在物理上限以內（例如球速不得超過 $120\text{ km/h}$）。
     - 得分與來回拍數是否吻合 USA Pickleball 官方計分規則。
   - 若數據超乎常理，自動判定無效並捨棄。

---

### 【長期方案】：校園單一登入與身分綁定 (SSO / OAuth 2.0)
1. **Google OAuth 2.0 / 中興大學校園登入**：
   - 玩家透過學校提供的 Google Workspace 帳號或校園帳號登入。
   - 客戶端取得短期有效的 JWT Token。
2. **權限隔離**：
   - 每個寫入操作均驗證 Token 身分，確定該 `playerId` 確實對應當前登入者。
   - 徹底杜絕女巫攻擊（一人註冊千個虛擬 ID 刷讚）與冒名竄改他人資料。

---

## 四、 結論

目前系統的資安設計對於**一般校園休閒體驗與防止低階無意篡改**具備基礎能力（HMAC、防重放、樂觀更新體驗極佳）。  
然而，若後續專案將舉辦全校性公開競技、排位賽或具備獎勵性質的競賽，**強烈建議優先導入「短期方案：Cloudflare Workers 反向代理」**，即可在不改動 Google Sheets 與 GAS 核心邏輯的前提下，達成端點隱藏、密鑰保護與防 DoS 攻擊的關鍵安全飛躍。
