# 匹克球 Web 體感遊戲 - V5 世代整合報告與變更日誌 (Changelog)

這份文件用於追蹤與記錄 V5 全新世代專案中所有針對 `匹克球V5.0.0.html` 及後續版本的修改紀錄。

---

## 版本管理規範
1. **絕不直接覆蓋歷史版本**：舊有的 `匹克球V4/` 目錄與歷史檔案（如 `匹克球V4.1.5.html`）永遠保持凍結與備份。
2. **單向遞增規範**：後續微調時，需複製當前最新版本並遞增版本號（例如：`匹克球V5.0.0.html` -> `匹克球V5.0.1.html`）。
3. **零退化承諾 (Zero Regression)**：所有 UI/UX 與規則系統之增強，絕不得影響既有 Three.js 3D 渲染、MediaPipe 攝影機體感、馬格努斯側旋物理、動力鏈日誌與數位孿生核心功能。

---

## 變更歷史 (Changelog)

### [v5.0.0] - 2026-09-10
**目標檔案**：`匹克球V5.0.0.html`（以 `匹克球V4.1.5.html` 為完整基礎進行全新世代升級，獨立存放於 `test systeam/匹克球V5/`）

#### 1. 📖 2026 USA Pickleball 官方競賽手冊彈窗 (`#rules-modal`)
- **6 大黃金單打規則卡片化**：
  - **01 計分與發球權 (Score & Service)**：單打 11 分制、領先 2 分獲勝；採發球得分制（Side-out Scoring）；偶數分由右發、奇數分由左發。
  - **02 合法下手發球 (A Legal Serve)**：揮拍自下而上、擊球點低於腰部、拍頭低於手腕；雙腳底線後方；對角越過廚房線；10 秒發球計時。
  - **03 雙彈跳核心法則 (Two-Bounce Rule)**：接發球必須落地彈跳一次、回擊後發球方亦必須落地彈跳一次，兩跳完成始開放凌空截擊 (Open Play)。
  - **04 非截擊區 / 廚房 (The Kitchen / NVZ)**：網前 7 呎 (2.13m) 區域嚴禁凌空截擊；落地後方可入內擊球；慣性衝進廚房亦判定犯規。
  - **05 界內外與球網 (In, Out & Net)**：邊線壓線算界內；發球壓廚房線算界外；擦網過網合法活球；允許繞柱球 (ATP)。
  - **06 常見違規失誤 (Rally Faults)**：觸身球判負、觸網判負、連擊違規、嚴禁摔拋球拍。
- **導覽與選單雙入口**：
  - 頂部導覽列 `#nav` 增設高亮快捷按鈕：`📖 規則`。
  - 設定選單第 2 層（遊戲與視角抽屜）新增：`📖 2026 官方競賽手冊`。
- **官方規範串接**：底部提供直連 USA Pickleball 官方完整手冊之外部連結按鈕。

#### 2. 🏟️ 3D 球場網前 7 呎非截擊區 (Non-Volley Zone) 立體視覺標註
- 於 Three.js 3D 球場網前兩側各 2.13m（7 英尺）的廚房區地面上，動態繪製「`7 FT · NON-VOLLEY ZONE (KITCHEN)`」立體透視文字與微光警示標記。
- 讓球員在 3D 透視視角下能一眼看清廚房區邊界，大幅強化賽場真實感與防踏線指引。

#### 3. 🚨 虛擬裁判浮動即時裁決膠囊 (`#referee-announcement`)
- **無障礙標準規格**：採用 HTML5 `aria-live="polite"` 與 `role="status"` 規範。
- **動態狀態機連動**：
  - 自動連結所有比賽即時事件：發球失誤、界外出界、廚房區違規 (Kitchen Fault)、雙彈跳違規、ACE 絕殺、得分與發球權轉換 (SIDEOUT)。
  - 一般得分/換發呈現競技螢光黃綠（`--lime`）微光；違規失誤自動切換為警戒紅光暈（`fault` 樣式）。
  - 懸浮於畫面中上方，2.4 秒後平滑向上縮回淡出，不遮蔽核心擊球視野。

#### 4. 🏃 專業運動排印系統 (Barlow Condensed & Tabular Nums)
- **引入專用運動字型**：載入 Google Fonts `Barlow Condensed`（字重 600, 700, 800）。
- **3D 看台計分板數字升級**：比分數字改以 `Barlow Condensed` 粗體渲染（`bold 64px "Barlow Condensed"`），配色更新為競技黃綠。
- **速度指示膠囊抗跳動**：左下角浮動迷你膠囊（`#speed-hud-mini`）全面啟用 `font-variant-numeric: tabular-nums`，徹底解決球速在位數切換時造成的排版抖動問題。

#### 5. 📱 手機端直向優雅旋轉提示 (`#rotate-prompt`) 與安全區域適配
- **直向旋轉引導層**：在螢幕寬度 $\le 900\text{px}$ 且直向握持（`orientation: portrait`）時，自動浮現優雅毛玻璃提示層，引導使用者轉為橫向以解鎖最寬廣 3D 視野；附設「仍以直向遊玩」略過按鈕。
- **全域 Safe-Area 適配**：
  - 虛擬搖桿：`bottom: calc(18px + env(safe-area-inset-bottom))`、`left: calc(16px + env(safe-area-inset-left))`。
  - 速度指示膠囊：`bottom: calc(14px + env(safe-area-inset-bottom))`、`left: calc(14px + env(safe-area-inset-left))`。
  - 杜絕在 iPhone 瀏海、動態島與底條手勢區發生遮擋。

#### 6. 🕹️ 虛擬搖桿金屬內凹雕刻感與硬體加速
- **Bevel Inset 立體陰影**：底盤導入 `inset 0 0 0 8px rgba(211, 243, 108, 0.06)`，模擬精密運動儀器底座的微光雕刻質感。
- **硬體渲染加速**：旋鈕（`#nub`）賦予 `will-change: transform`，確保在行動端激烈操作走位時維持穩定的 60fps 滑順反饋。

#### 7. 🛡️ 既有核心 100% 零退化驗證 (Zero Regression Guaranteed)
- **3D 空間與鏡頭**：Three.js 場景、主角史蒂夫、匹克鵝 AI、相機平滑動態追蹤（Lerp Tracking）全數正常。
- **AI 體感與物理運算**：MediaPipe 人體骨架感應、揮拍速度差分 EMA 濾波、馬格努斯側旋香蕉弧線物理 100% 保留。
- **數據審計與數位孿生**：動力鏈審計日誌（Kinematic Audit Log）、Google Sheets 雲端孿生、5 維雷達圖社交名片、3 站式 Spotlight 新手導覽全數完美運作。

---

### [v5.0.1] - 2026-09-10
**目標檔案**：`匹克球V5.0.1.html`（基於 `v5.0.0` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🪟 示範講解對話框升級為高對比亮白毛玻璃卡片 (`#demo`)
- **視覺調色與對比強化**：
  - 將電腦動作示範時底部彈出的解說對話框（`#demo`）由原先偏暗的夜空藍底色，全面重構為**純白微透毛玻璃卡片**（`background: rgba(255, 255, 255, 0.96); backdrop-filter: blur(16px);`）。
  - 外框邊界使用 `border: 1.5px solid rgba(255, 255, 255, 0.95)` 搭配高階層雙重落塵陰影（`box-shadow: 0 18px 45px rgba(0,0,0,0.35), 0 2px 12px rgba(0,0,0,0.08)`），在 3D 深色球場與看台背景上呈現極佳的層次浮凸感。
- **排印層次重整**：
  - 步驟膠囊（`#d-tag`）：採用競技螢光黃綠（`--lime`）實心底色搭配深墨黑文字（`#1e293b`），醒目指示目前示範進度（例如 `🎬 STEP 1 預備`）。
  - 說明內文（`#d-cap`）：字體加大至 `16.5px`，使用高對比深墨黑（`#0f172a`），粗體加強，大幅提升閱讀清晰度。
  - 跳過提示（`#d-skip`）：文字改為柔和岩灰（`#64748b`），鍵盤按鍵 `<kbd>` 採用淺灰實體按鍵質感（`background: #f1f5f9; border: 1px solid #cbd5e1;`）。

#### 2. ✨ 新手互動導覽講解卡片同步純白亮化 (`.spotlight-card`)
- 同步將 3 站式 Spotlight 互動導覽的說明卡片（`.spotlight-card`）升級為與 Sideout 風格一致的高質感純白毛玻璃卡片（`rgba(255, 255, 255, 0.96)`）。
- 標題與說明文字轉為高對比深色階（`#0f172a` 與 `#475569`），按鈕互動色彩微調，整體視覺系統一致且明亮透徹。

---

### [v5.0.2] - 2026-09-10
**目標檔案**：`匹克球V5.0.2.html`（基於 `v5.0.1` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🌪️ 馬格努斯側旋物理重構 (1.2m~1.8m 顯著香蕉弧線)
- **修正側向加速度公式**：
  - 移除原先三次方公式 `(2.35 * spin)^3` 造成的過度衰減（原先 spin=0.35 時偏折不到 18 公分，在 3D 透視下肉眼幾乎不可見）。
  - 升級為高靈敏複合加速：`magnusAcc = (spin * 9.6 + sign(spin) * spin^2 * 4.8) * (0.8 + 0.9 * curveFlightFactor)`。
  - 將側向速度上限由 `±2.8 m/s` 擴展至 `±6.5 m/s`，使顯著側旋球在空中能走出 **1.2 ~ 1.8 公尺** 的大曲度香蕉弧線！
- **側旋感應靈敏度大幅優化**：
  - 手機/滑鼠滑動側旋估算分母由 1400 大幅下修至 480（`SWIPE.vx / 480`），稍有橫向刷拍即可輕鬆打出高品質弧線球。
  - 側旋顯著時，球體發光氣流（`ballGlow`）自動切換為高彩度螢光色（右側旋黃綠光、左側旋霓虹靛紫），並連動虛擬裁判廣播 `🌪️ 側旋弧線 (SPIN)`。

#### 2. 🏸 擊球力道階梯與智慧優先權重整 (解決力道失控與掛網)
- **拔除力道覆蓋 Bug**：
  - 修正原先「快速滑動放開時，0.22 秒的微弱預設蓄力值（`swingP < 10%`）強制覆蓋高速滑動推力」之邏輯缺陷。
  - 建立嚴格的力道階梯：當偵測到高速推拍（`SWIPE.peakVy > 80`）時，**100% 優先採納真實滑動衝量**，不再發生快速推拍卻軟弱落網的異常。
- **動態出球初速解算器 (`solveArc(..., speedScale)`)**：
  - 軌跡解算不再寫死 11 m/s 固定飛行時間，改為支援動態初速縮放：
    - **輕推短球 (Dink, <25%)**：柔和慢速（初速縮放 0.8x），高拋物線越網落入對面廚房。
    - **中速後場球 (Drive, 25%~65%)**：平直深遠（初速 0.95x ~ 1.2x），落入底線邊緣。
    - **高速抽殺 (Power Shot, >65%)**：極速破空（初速 1.22x ~ 1.6x），具備極高球速威脅。
- **空氣阻力 (Air Drag) 物理模擬**：
  - 球體在每步步進時施加空氣阻力衰減（`vel *= (1 - 0.12 * h)`），重球出拍破空、尾端自然減速，彈跳更沉穩。

#### 3. 🏃 人物走位物理加速度與煞車慣性 (告別定速瞬移)
- **引入物理速度向量 (`playerVel`)**：
  - 徹底移除 `s = baseSpeed * dt` 瞬時等速位移，改由速度與加速度積分計算。
  - 起跑賦予 **26 m/s² 物理加速度**，鬆開時由 **18/s 摩擦阻尼 (Friction Damping)** 順勢煞停。
  - 加入斜向移動正規化（$1/\sqrt{2}$），解決斜走超速問題。
  - 鍵盤 WASD 與虛擬搖桿操作具備真實人體的起步爆發力與煞車慣性，走位手感流暢滑順。

---

### [v5.0.3] - 2026-09-10
**目標檔案**：`匹克球V5.0.3.html`（基於 `v5.0.2` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🤖 AI 對手三級難度設定 (零額外引擎負擔，純乘數機率調控)
- **三段式難度預設 (`DIFF_PRESETS`)**：
  - 🟢 **初階 (Easy)**：`missMultiplier = 4.0`，`speedScale = 0.75`。以第 4 關為例，基礎失誤率提升至 **80%**，移速降至 3.9 m/s，精準達成「約 5 球回 1 球」的新手友善水準。
  - 🟡 **中等 (Medium)**：`missMultiplier = 2.5`，`speedScale = 0.90`。失誤率提高至 **50%~67%**，移速 4.7 m/s，呈現「約 2~3 球回 1 球」的均勻拉鋸節奏。
  - 🔴 **困難 (Hard)**：`missMultiplier = 1.0`，`speedScale = 1.00`。維持完整防守機率（失誤率 20%，魔王 14%），移速全開。
- **無負擔輕量整合**：
  - 僅在 `updateGoose()` 中的 `missRate` 與 `spd` 套用純乘數運算，零外部推論引擎，完全不消耗手機有限的 CPU。
  - 僅對 Stage 4（對決）與 Stage 5（魔王）生效，教學關卡（Stage 1~3）維持標準引導行為。
  - 狀態自動持久化至 `localStorage('nchu_pb_diff')`，預設為「初階」。
- **雙重 UI 入口**：
  - 於「關卡抽屜（data-sec="stage"）」與「設定 > 遊戲與視角（settings-l2-game）」皆配置 3 顆切換膠囊按鈕（🟢 初階 | 🟡 中等 | 🔴 困難），並連動虛擬裁判廣播 toast 提示。

#### 2. 📱 手機視角與 FOV 自動響應優化 (告別小螢幕壓迫感)
- **自適應攝影機視野 (Responsive FOV)**：
  - 螢幕寬度小於 768px（手機直橫向與小平板）時，視角視野自動從 50° 放寬至 **62°**（`getResponsiveFOV()`）。
  - `window.resize` 時同步即時更新 `cam.fov` 與投影矩陣，小螢幕球場不再顯得狹窄緊繃，球體與人物比例清晰放大。
- **動態自適應經典視角 (Mode 0)**：
  - 手機端鏡頭座標由原先過高過遠的 `(0, 9.4, 15.4)` 下移並拉近至 `(0, 6.2, 11.2)`，視線平視 `(0, 0.82, -0.7)`，大幅增強臨場感與擊球點判斷。
  - 桌機端維持 `(0, 8.2, 13.6)` 全景廣角俯瞰。
- **浮動快速切換視角按鈕 (`#quick-cam-btn`)**：
  - 於 HUD 右上方常駐半透明磨砂膠囊按鈕（`🎥 經典 / 俯瞰 / 越肩 / 轉播 / 自訂`），手勢一觸即可在 5 種視角間無縫循環，不必再繁瑣地層層打開設定選單。

#### 3. 🖐️ HUD 面板長按拖曳系統 ＆ 搖桿重疊 Bug 修復
- **修復手機左下角 `#joy` 與 `#speed-hud-mini` 重疊衝突**：
  - 原先速度膠囊與虛擬搖桿皆固定在左下角，導致膠囊觸控覆蓋搖桿觸發區。
  - v5.0.3 將 `#speed-hud-mini` 在手機端預設改置於右下角（`bottom: calc(14px + safe-area); right: calc(14px + safe-area);`），桌面端維持左下角，徹底釋放左下角搖桿操控空間。
- **純觸控/滑鼠長按可拖曳架構 (`initDraggableHUD`)**：
  - 為 `#info`（關卡資訊面板）與 `#speed-hud-mini`（迷你速度指示器）加入輕量手勢監聽。
  - 長按 **360ms** 即觸發拖曳模式：元素微幅放大 105%、外圈亮起藍色霓虹光暈、觸發輕微觸覺震動（`navigator.vibrate(28)`）。
  - 滑動時即時跟隨指尖，支援全螢幕安全邊界防出界鉗制。
  - 放開時將座標自動寫入 `localStorage('nchu_pb_hud_positions')`，玩家自訂位置永久記憶保留。
  - 短按（<360ms）依舊完美執行原本的「面板展開/收折」功能，手勢判定零衝突。

---

### [v5.0.4] - 2026-09-10
**目標檔案**：`匹克球V5.0.4.html`（基於 `v5.0.3` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🏓 初階 AI 失誤率大幅平衡調降 ＆ 來回抽球互動感優化
- **解決初階太廢無互動感問題**：
  - 原先 v5.0.3 初階設定為 80% 失誤（高球速更達 91%），導致 AI 幾乎每球都掛網或出界，玩家完全無法享受長拍來回抽球的樂趣。
  - v5.0.4 將初階失誤乘數下調為 **1.35x**（第 4 關基礎失誤率由 80% 大幅修正為 **27%**），**AI 成功回球率高達 ~73%**！
  - 移速調整為適中的 **0.88x**，既不會瘋狂衝刺突襲，也不會慢到接不到球。
  - **初階友善落點導向 (`planShot`)**：在初階模式下，AI 回球落點水平分散度由 `±1.9m` 縮小至 `±0.7m`（`pPos.x ± 0.7m`），回球主動朝玩家周圍舒適送球，大幅提高連續來回多拍對抗的爽感與互動成就感！
- **全新三級參數重平衡**：
  - 🟢 **初階**：`missMult = 1.35`，`speed = 0.88x`，友善落點，回球率 ~73%（主打練習與連續抽球互動）。
  - 🟡 **中等**：`missMult = 1.00`，`speed = 1.00x`，標準落點，回球率 ~80%（均衡競賽節奏）。
  - 🔴 **困難**：`missMult = 0.65`，`speed = 1.15x`，邊角刁鑽落點，回球率 ~87%（高強度魔王挑戰）。

#### 2. 🎯 AI 難度入口超直覺醒目化 (解決找不到選項的問題)
- **第一醒目入口：左上角 `#info` 關卡卡片直接常駐切換列**：
  - 在關卡卡片最下方加入 `#info-diff-row`，常駐 3 顆高對比膠囊按鈕：
    `🤖 難度: [🟢初階] [🟡中等] [🔴困難]`
  - 不需要點開任何層層折疊的選單抽屜，只要一進遊戲抬頭看卡片，一眼就能看到並直接點擊切換！
- **第二醒目入口：右上方浮動膠囊按鈕 (`#quick-diff-btn`)**：
  - 在右上方 `🎥 經典` 視角按鈕正下方，常駐一顆精巧的 `🤖 初階` 快捷膠囊按鈕。
  - 輕觸即在「🟢初階 ➔ 🟡中等 ➔ 🔴困難」之間直接循環切換，按鈕外框與文字自動連動綠/黃/紅主題色，並跳出虛擬裁判 Toast 廣播提示。
- **第三入口**：原有的關卡抽屜與設定抽屜依舊完整保留同步連動。

---

### [v5.0.5] - 2026-09-10
**目標檔案**：`匹克球V5.0.5.html`（基於 `v5.0.4` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🔍 徹底根治「手機畫面上全部小到靠杯」問題
- **痛點深入頗析**：
  1. **3D 鏡頭過遠與 FOV 反向縮小**：先前預設鏡頭距離高達 11.2~13.6 公尺，在手機直向（寬高比 ~0.46）極端狹長的螢幕下，未經橫向視野補償的相機導致整個球場縮成一條又細又長的中間帶，人物與球體只佔螢幕十幾像素！
  2. **UI 字體與元素過於微型**：卡片、按鈕、說明文字大多落在 9px~11px，在手機高 DPI 螢幕上細小難以辨識，導航欄甚至被壓扁在 155px 內。

#### 2. 🎥 智慧動態相機解算器 (`getResponsiveCameraConfig`)
- **直向模式 (Portrait)**：
  - **自動長寬比視野解算**：以球場 6.1m 寬度剛好貼合手機螢幕邊界為基準，自動解算垂直 FOV（`targetHFOV = 43°`，動態換算垂直 FOV 至 70°~80°）。
  - **鏡頭拉近超過 3.5 公尺**：鏡頭距離由 `11.2m` 降至 **`8.4m`**，高度由 `6.2m` 降至 **`4.0m`**。
  - **效果**：球場兩側線條剛好滿版鋪滿手機左右，人物與對手尺寸**放大超過 200%**，再也沒有浪費兩側大片空綠地的縮小感！
- **橫向模式 (Landscape)**：
  - 鏡頭座標貼近至 `(0, 3.2, 7.4)`，垂直 FOV 調緊為 `42°`（長焦鏡頭特寫感），呈現極致飽滿的電視轉播沉浸感。
- **球體視覺等比放大 45% + 霓虹光暈倍增**：
  - 手機端球體模型視覺比例由 `1.0x` 提升至 **`1.45x`**，光暈尺寸放大至 `BALL_R * 10`，透明度提升至 `0.45`，即使小螢幕也能一眼鎖定黃綠色匹克球！

#### 3. 📱 全局 UI 與文字排版 30%~50% 全面等比放大
- **頂部導航列 (`#nav`)**：解除 155px 寬度封鎖，改為滿版左右 14px 安全邊界可滑動膠囊欄，按鈕字體放大至 **13px bold**，按鈕內距增大，單手好點。
- **關卡卡片 (`#info`)**：標題由 16px 放大至 **19px bold**，任務進度與內文放大至 **14px**，難度膠囊放大至 **12px**。
- **右上角快捷按鈕 (`#quick-cam-btn` & `#quick-diff-btn`)**：加大至 **12.5px** 舒適藥丸形狀，不易誤觸。
- **虛擬搖桿 (`#joy` & `#nub`)**：直徑由 106px 擴展至 **122px**，中心旋鈕由 40px 擴大至 **48px**，大拇指操控回饋感大幅躍升。
- **速度膠囊 (`#speed-hud-mini`)**：字體加大至 **13.5px bold**，數據一目了然。

---

### [v5.0.6] - 2026-09-10
**目標檔案**：`匹克球V5.0.6.html`（基於 `v5.0.5` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🎯 全新極簡頂部三合一操作膠囊列 (`#top-actions`)
- **徹底拔除舊式繁雜混亂的頂欄**：
  - 原先畫面上方同時擠滿了「6 個按鈕的舊式導航橫條 + 關卡資訊卡 + 視角快捷鈕 + 難度快捷鈕」，畫面雜亂擁擠且極易誤觸。
  - v5.0.6 全面隱藏舊式繁雜橫條，將所有頂部快捷操作統一濃縮為**右上角 3 顆極簡直覺的微光磨砂膠囊**：
    1. `[ 🎥 經典 ]`：單手輕觸立即循環切換 5 種 3D 視角。
    2. `[ 🤖 初階 ]`：單手輕觸立即輪替切換 3 種 AI 對抗難度（綠/黃/紅即時變色）。
    3. `[ ☰ 選單 ]`：極度吸睛的藍紫漸層膠囊，輕點即開啟全新現代化控制抽屜！

#### 2. 📱 現代化人體工學底部控制抽屜 (`#main-menu-overlay`)
- **為手機單手操作量身打造**：
  - 點擊 `[ ☰ 選單 ]` 後，全螢幕背景柔和暗化模糊（`backdrop-filter: blur(8px)`），由**螢幕底端平滑滑出精緻的玻璃擬態卡片**（符合大拇指自然活動人體工學，不再需要費力伸手戳螢幕頂端）。
- **四大多功能模組分區，大按鈕好按零迷路**：
  1. 🎮 **遊戲關卡切換**：5 顆獨立大網格按鈕（1 發球、2 雙彈跳、3 廚房區、4 對決、5 魔王），目前進行關卡高亮發光，並附帶一鍵「重新觀看示範」。
  2. 🤖 **AI 對手難度**：3 顆大膠囊按鈕（🟢 初階高回球 / 🟡 中等對抗 / 🔴 困難魔王）。
  3. 🎥 **3D 攝影視角**：清晰列出「經典追蹤」、「戰術俯瞰」、「沉浸越肩」、「電視轉播」與「🎨 自訂調節視角」。
  4. 📖 **規則手冊與系統功能**：收納「2026 官方競賽規則」、「英雄榜與球友」、「音效與音量」、「技術原理」、「編輯個人檔案」與「🏳️ 棄賽算分」。
- **極致順暢的互動體驗**：
  - 點擊抽屜外任意空白處或右上角 `✕` 即可順滑收回，完全杜絕遊戲進行中因操作選單而誤揮拍或遮擋球場的問題！

---

### [v5.0.7] - 2026-09-10
**目標檔案**：`匹克球V5.0.7.html`（基於 `v5.0.6` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🎬 示範期間自動全面隱藏所有選單與 HUD (視野 100% 零遮擋)
- **問題根因**：原先開頭播放「新手手指側旋滑動引導」或各關「匹克鵝戰術示範」時，畫面上方的頂欄膠囊、關卡卡片、搖桿與速度指示器全數顯示在畫面上，嚴重遮擋了球場視野、揮拍動作與香蕉球弧線軌跡。
- **全新示範沉浸守護機制 (`body.demo-mode-active`)**：
  - 當「新手側旋指引動畫」或「關卡示範動畫」啟動時，系統自動套用 `demo-mode-active` 樣式。
  - **所有選單、頂欄按鈕、關卡資訊卡、虛擬搖桿與速度膠囊全數自動淡出隱藏 (`opacity: 0; pointer-events: none;`)**！
  - 畫面保持 100% 乾淨通透的電影級全景視野，讓玩家清楚觀摩側旋擊球軌跡、揮拍手勢與落點示範。
  - 玩家點擊螢幕開始遊戲（或示範播放完畢/點擊跳過）時，所有操作介面平滑淡入恢復，無縫銜接遊玩！

#### 2. ⚡ 控制選單抽屜點擊失效修復 (背景與卡片徹底解耦)
- **問題根因**：v5.0.6 原先的抽屜遮罩將 `closeMainMenu()` 綁定在整個外層容器上，在手機觸控點擊時，子層按鈕的點擊事件容易誤觸外層遮罩的關閉邏輯，導致「選單內完全按不到」。
- **全新解耦架構**：
  - 將「背景遮罩 (`.menu-backdrop`)」與「抽屜卡片主體 (`.menu-sheet`)」徹底分離為獨立同級元素，點擊卡片絕不可能穿透或冒泡至背景關閉事件。
  - 為抽屜卡片及內部所有按鈕強制套用 `pointer-events: auto !important` 與 `touch-action: manipulation`，杜絕任何觸控延遲與手勢衝突。
  - 在 `openMainMenu()` 加入 `void overlay.offsetWidth` 強制瀏覽器重繪，保證抽屜在 iOS Safari 與 Android Chrome 彈出時 100% 立即具備完整點擊響應。

---

### [v5.0.8] - 2026-09-10
**目標檔案**：`匹克球V5.0.8.html`（基於 `v5.0.7` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🔍 側旋球自動偏向單側之物理成因剖析 (Root Cause Analysis)
- **問題根因 1：`hitOffset` 拍面偏心率污染旋轉量**：
  在 `tryHit()` 擊球邏輯中，原先將 `hitOffset = (b.x - p.x) / 0.4`（球相對於球拍中心的偏離距離）乘以 `0.4` 強制累加進 `spin`。由於玩家在發球或接發時往往站在球場一側（如右發球區），球拍與球心自然存在橫向偏距，導致系統在每一次擊球時「永久自帶強烈的右側旋偏轉」，即使玩家完全筆直推擊，球也會自動強行拐彎！
- **問題根因 2：`touchDeltaX` 觸控起始座標跳動**：
  手機玩家在單手握持或大拇指點擊時，首次碰觸螢幕的 X 座標通常落在手機右半邊（`touchDeltaX > 0`），原先在 `doServe()` 與 `tryHit()` 中將 `touchDeltaX * 12.0` 當成橫向揮拍加速度，導致手機端玩家只要一碰螢幕，就會被硬塞一個向右側旋的起始旋轉！
- **問題根因 3：`Physics.step` 馬格努斯加速度係數過激**：
  原先 `magnusAcc = (spin * 9.6 + spin^2 * 4.8) * 1.7`，加速度最高可達 14 m/s²（比地心引力 9.8 還大），只要有些微微小的 0.05 偏轉，就會在飛行過程中被數倍放大，直接向外側劇烈漂移超過 2 公尺飛出界外。

#### 2. 🎯 直推 100% 絕對筆直零偏漂 (Straight Hit Deadzone)
- **徹底拔除 `hitOffset` 與 `touchDeltaX` 對旋轉量的污染**：
  - 發球 `doServe()` 與擊球 `tryHit()` 中的 `spin` 數值，徹底切斷與擊球點偏心率及初次觸控位置的耦合。
  - 擊球點偏心只保留在落點目標瞄準 `tx`，旋轉量 `spin` 回歸純粹的「玩家揮拍橫切動作」。
- **直推保證筆直死區 (`|SWIPE.peakVx| <= 95 px/s`)**：
  - 當玩家向上推球、正常直推揮拍時，只要橫向滑動速度未達刻意側刷門檻（$\le 95\text{ px/s}$），`spin` 強制歸零 (`spin = 0`)。
  - 在物理循環 `Physics.step()` 中，當 `|spin| <= 0.06` 時，立即強制 `this.spinInc = 0`，球體 100% 沿直線軌道彈道飛行，絕對不會發生任何自動偏漂！

#### 3. 🌪️ 刻意側刷之香蕉弧線平穩自然化 (Magnus Curve Recalibration)
- **自然流體側彎範圍控制在 0.7m ~ 0.9m**：
  - 當玩家大拇指刻意「向左橫刷」或「向右橫切」（橫向滑動峰值速度 $> 95\text{ px/s}$）時，系統才啟動側旋計算：
    `spin = dir * Math.min(1.0, (|brushVx| - 95) / 450)`。
  - 馬格努斯橫向氣動力加速度適度收斂為 `(spin * 4.4 + spin^2 * 2.2) * (0.75 + 0.65 * curveFlightFactor)`，側向增量上限由 `6.5 m/s` 收斂至 `3.2 m/s`。
  - 側旋球在通過網前時劃出極具觀賞性且符合真實物理手感的平滑「香蕉球弧線」，既有挑戰性又可被精準預測，大幅提升控球樂趣與競技體感。

---

### [v5.0.9] - 2026-09-10
**目標檔案**：`匹克球V5.0.9.html`（基於 `v5.0.8` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🎾 匹克球多孔真實空氣動力學模型 (Perforated Ball Aerodynamics)
- **問題根因剖析**：
  - 匹克球與桌球、網球存在本質差異：匹克球為帶有 26~40 個穿孔的硬質塑料球體。氣流會直接穿透球體內部，破壞球體周圍連續邊界層的環流（Circulation），使得馬格努斯效應（流體力學偏轉）遠比乒乓球或網球微弱得多（在真實賽場 13.4 米飛行距離中，極致側旋的橫向偏轉通常僅 10~25 公分，絕不可能像桌球一樣劃出 1~2 米的誇張香蕉弧線）。
  - 原先的加速度係數高達 4.4~9.6，且側向速度上限高達 3.2 m/s，導致玩家即使只是隨手擊球，球體便會像乒乓球大拐彎一樣到處亂飛。
- **空氣動力學參數大尺度收斂至真實賽場**：
  - **橫向馬格努斯加速度係數收窄至 1/5**：由 `(spin * 4.4 + spin^2 * 2.2)` 調降為 `(spin * 0.85 + spin^2 * 0.35)`。
  - **側向累積速度上限收縮**：由原本的 `3.2 m/s` 嚴格收斂至 `0.45 m/s`（極限全場飛行橫向總位移僅約 `0.15m ~ 0.22m`，呈現真實自然、點到為止的匹克球切球微弧線）。
  - **多孔擾流空中自然衰減**：加入飛行中自旋衰減 `this.spin *= (1 - 0.35 * h)`，模擬穿孔空氣洩壓阻力。
  - **落地滑動歸零**：球體落地彈跳瞬間強制 `this.spinInc = 0`，側旋衰減至 `0.35`，徹底杜絕球落地後在地面繼續橫向滑移的異常現象。

#### 2. 🎯 擊球落點瞄準 (tx) 穩定化，杜絕隨意揮拍到處亂飛
- **問題根因剖析**：
  - 原先在 `tryHit()` 中，落點瞄準 `tx` 疊加了 `(SWIPE.peakVx / 550) * 3.05`、`padX * 1.8` 以及 `(b.x - p.x) * 1.6`。當玩家在手機螢幕上正常向上揮拍時，大拇指人體工學的自然微斜（約 15°~20°）就會產生 200 px/s 的橫向分速，導致 `tx` 被直接甩至 2.5m~3.3m（球場邊緣或界外），加上過激的側旋，造成「單純揮拍回擊球卻滿場亂飛」。
- **瞄準穩定化與安全邊界防護**：
  - 滑動指向係數由激進的 `(SWIPE.peakVx / 550) * 3.05` 收斂為平穩的 `clamp((peakVx / 800) * 0.75, -0.85, 0.85)`。
  - 拍面擊球導向收斂為 `clamp((padX * 0.45) + ((b.x - p.x) * 0.35), -0.65, 0.65)`。
  - **球場安全邊界**：落點 `tx` 嚴格限制在 `[-COURT_W/2 + 0.55, COURT_W/2 - 0.55]`（距離左右邊線至少保留 0.55m 安全寬度），保證所有正常回擊球穩穩留在對方場內，手感極度紮實可控。

#### 3. 👆 手勢判定：徹底分離「直推回擊」與「刻意側切」
- **雙重門檻過濾**：
  - 原先僅以 `|peakVx| > 95 px/s` 作為判定，而手機輕微滑動即可突破 150 px/s。
  - 新增比例判定：只有當橫切速度滿足 `|peakVx| > 280 px/s` 且橫向速度超過垂直速度 45%（`|peakVx| > |peakVy| * 0.45`）時，系統才認定為刻意側切（Slice）；凡向上推球（`peakVy` 為主）一律判定為直球，`spin` 100% 保持為 `0`。
  - 側旋上限由原本的誇張 `1.0` 大幅收窄至 `0.28`（發球上限 `0.22`），回歸匹克球真實物理手感。

#### 4. 🪿 匹克鵝回球側旋重置 (Goose Return Spin Reset)
- 原先匹克鵝回擊時未清除球體上的 `PH.spin` 與 `PH.spinInc`，導致匹克鵝回球時球體繼承了玩家上一拍的側向速度，在場上產生怪異拐彎。
- 匹克鵝回擊瞬間強制 `PH.spin = 0; PH.spinInc = 0;`，保證 AI 回球彈道乾淨平穩、落點明確。

#### 5. 🏓 發球對角有效發球區鎖定 (Cross-Court Serve Target)
- 手機觸控發球時，落點目標基準正式錨定至規則規定的對角有效發球區 `diagSign() * (COURT_W / 4)`，搭配玩家拍面微調，發球自然滑向對角發球方塊，不再無故發出界或瞄偏。

---

### [v5.0.10] - 2026-09-10
**目標檔案**：`匹克球V5.0.10.html`（基於 `v5.0.9` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🎬 示範教學系統全面重生 (Text, 3D Guides & Finger Gesture)
- **問題根因剖析**：
  - 在 v5.0.7 中為了避免操作選單遮擋示範，將 `body.demo-mode-active #hud` 設為 `opacity: 0`。然而教學文字卡片 `#demo` 與浮水印恰好位在 `#hud` 容器內，導致整張教學說明卡片被連坐隱藏；
  - 3D 軌跡引導線 `arc` 與落點光圈 `zoneServe` 原先僅在 `state === 'SERVE_READY'` 玩家發球時開啟，在 `state === 'DEMO'` 示範期間全數關閉；
  - 新手手指滑動教學卡片 `#finger-tutorial` 過去在任何螢幕觸碰時便被立即 `dismiss` 關閉，造成關卡示範時完全沒有手勢引導動畫，使用者看到的示範變得「完全空白」。
- **教學文字卡片 (`#demo`) 重生與操作優化**：
  - 精確鎖定隱藏目標：只隱藏與示範無關的遊戲控制項（`#top-actions`, `#joy`, `#speed-hud-mini`, `#bottom`, `#nav`, `#nav-sheet`, `#info-diff-row`, `#referee-announcement`），保留 `#demo` 與教學浮水印。
  - `#demo` 卡片賦予頂層優先權（`z-index: 10000`），純白高對比磨砂玻璃，置中於畫面底部（`bottom: 24px`），醒目呈現步驟徽章（`#d-tag`：STEP 1 站位預備、STEP 2 擊球點高度、STEP 3 直推發球、STEP 5 側切發球等）與中文詳細解說（`#d-cap`）。
  - 新增醒目 `[ 跳過示範 › ]` 按鈕，點擊立即無縫銜接遊玩。
- **3D 動作軌跡引導線 (3D Guide Lines) 常駐高亮**：
  - 在示範狀態（`state === 'DEMO'`）下，自動開啟 3D 軌跡弧線 `arc`、目標落點區 `zoneServe`、落點環 `ringLand` 與球員站位藍光圈 `ringSpot`。
  - 角色揮拍前，系統即時解算自球拍至目標落點（`dDemoTgt`）的完整拋物線，以亮麗的翡翠青綠螢光色（`#3fe0c4`）穿透球網高亮指引，落點精準對齊發球區或廚房線外。
- **手指滑動手勢示範 (Finger Tutorial) 節奏同步連動**：
  - 在示範動作期間，手指滑動動畫（`#finger-tut-hand` 與 `#finger-tut-trail`）同步在教學卡片上方展示標準大拇指手勢：
    - 示範直推深球時：同步展示向上直線前推（`Upward Swipe`），對應 `1. 直推打深 (Power)` 膠囊高亮。
    - 示範香蕉微側旋時：同步展示右上微刷手勢（`Swipe Up-Right`），對應 `2. 右弧側旋 (Right Spin)` 膠囊高亮。
  - 點擊「跳過示範」或畫面任意處，示範文字、3D 引導線與手指示範同步優雅淡出，所有遊戲控制按鈕平滑淡入，100% 銜接開球！

#### 2. 🛠️ JavaScript 語法自動化嚴格驗證與登入載入凍結修復 (Load Freeze Fix)
- **問題根因**：初次構建時 `updateGuides` 函數置換邊界發生錯位，導致括號閉合不匹配（Unclosed Brace），造成瀏覽器在解析 JavaScript 時拋出語法錯誤中斷執行，頁面停在登入初始畫面。
- **修復與自動化防護**：
  - 透過語法括號計數器精確替換完整的 `function updateGuides(dt) { ... }` 定義區塊。
  - 引入 Node.js 本地語法檢測管線（`node -c`），對打包後整段 229,248 字元的 JavaScript 腳本執行 100% 零錯誤驗證（`0 errors / VALID JS SYNTAX`），確保遊戲點開即刻順暢啟動、完全無卡死。

---

### [v5.0.11] - 2026-09-10
**目標檔案**：`匹克球V5.0.11.html`（基於 `v5.0.10` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🛡️ 登入畫面最高層級隔離 (Login Overlay Absolute Isolation)
- **問題根因剖析**：
  - 啟動階段執行的 `initFingerTutorial()` 偷跑給 `<body>` 加上了 `demo-mode-active` 類別，導致教學文字卡片 `#demo`（`z-index: 10000`）與「🤖 電腦動作示範中」浮水印在**玩家尚未登入時就硬生生蓋在登入畫面上方**；同時手機直向旋轉提示 `#rotate-prompt`（`z-index: 9999`）也在直向握持時遮擋表單，造成登入畫面看似「被改壞/面目全非」。
- **隔離與修復機制**：
  - **登入最高層級權限**：將 `#login-overlay` 提升至絕對頂層（`z-index: 100000 !important;`）。
  - **登入期間全域靜音**：只要 `#login-overlay` 存在且未關閉，強制隱藏 `#demo`、`#demo-watermark`、`#finger-tutorial`、`#top-actions` 以及手機直向旋轉提示 `#rotate-prompt`。玩家在直向握持手機時能 100% 舒適填寫學號前 7 碼、下拉選擇系所與年級，完全不受任何多餘浮層干擾。
  - **拔除啟動偷跑**：`initFingerTutorial()` 僅負責註冊點擊關閉事件與確保預設 `.hidden`，絕不在開頭私自啟動 `demo-mode-active` 或手勢計時器。

#### 2. ⚡ 控制選單列解鎖與 100% 響應保證 (Menu Pill Unlocked & Interactive)
- **問題根因剖析**：
  - 原先在 `body.demo-mode-active` 樣式中將 `#top-actions` 列入了 `pointer-events: none !important; opacity: 0 !important;`，導致無論是偷跑的示範狀態還是正常關卡示範，右上角的「☰ 選單」按鈕都直接失去所有點擊事件，使用者怎麼點都沒有反應。
- **解鎖與無縫互動機制**：
  - **選單始終常駐高亮**：從 `demo-mode-active` 隱藏清單中正式剔除 `#top-actions`，給予 `z-index: 900 !important; pointer-events: auto !important; opacity: 1 !important;`，確保隨時皆可直接觸控點擊。
  - **觸控優化**：所有按鈕膠囊全面套用 `touch-action: manipulation !important;`，杜絕行動端 300ms 點擊延遲。
  - **點擊選單自動停止示範**：在 `openMainMenu()` 中加入 `if (demoOn) { skipDemo(); }`，玩家一旦點開「☰ 選單」，系統即刻優雅收起示範卡片，平滑展開全效能抽屜選單（支援即時切換關卡 1~5、切換 AI 對手難度 初階/中等/困難、切換 4 種攝影視角與開啟 2026 競賽規則手冊）。

---

### [v5.0.12] - 2026-09-10
**目標檔案**：`匹克球V5.0.12.html`（基於 `v5.0.11` 完整複製並另存新檔，嚴格遵守不覆蓋歷史檔案規範）

#### 1. 🪿 前三關新手教學 AI 極低失誤率 (Beginner Stage Low Miss Rate)
- **問題根因剖析**：
  - 前三關（1 發球、2 雙彈跳、3 廚房區）的核心目標是「教學引導與規則熟悉」，必須讓新手玩家有足夠的來回球進行練習；然而原先 `AI_MISS` 在第 2 關與第 3 關設定了 8%~9% 的失誤率，且若玩家回球速度稍快還會額外疊加 11% 失誤率，導致匹克鵝動不動就自己失誤結束回合，新手完全無法建立連續擊球手感。
- **失誤率大幅收斂至教學安全標準**：
  - 前三關失誤率全面降至極低標準（`AI_MISS = { 1: 0.0, 2: 0.025, 3: 0.025 }`，僅 2.5%，平均 40 拍才偶爾失誤一次）。
  - 取消前三關對高速球額外增加失誤率的懲罰，僅在第 4 關對決與第 5 關魔王保留高速回球失誤加成。
  - **失誤表現友善化**：前三關的偶發失誤一律改為「底線微出界（`aiShot.z = HALF_L + 0.65; toast('🪿 匹克鵝回擊微出界', '偶發失誤')`）」，絕對不再在網前狼狽自爆掛網，保持新手教學的正向節奏。

#### 2. 🍳 第三關廚房吊球掛網徹底修復 (Kitchen Dink Trajectory Net-Clearance)
- **問題根因剖析**：
  - **落點過於貼網**：原先 `planShot()` 的廚房落點公式為 `0.55 + Math.random() * (KITCHEN_D - 0.95)`（0.55m ~ 1.73m），當落點隨機到 0.55m ~ 0.95m 時，落點距離球網過近；
  - **流體空氣阻力衰減導致下沉**：物理引擎中包含每步 `(1 - 0.12 * h)` 的空氣阻力，飛行過程中前進水平分速 $v_z$ 會隨時間衰減，使得球通過球網的時間 $t_n$ 比 `solveArc` 無阻力預測的時間更長，重力下墜位移大幅增加，導致球體在跨網瞬間高度跌落至 0.77m ~ 0.98m（低於網高 0.914m + 球半徑 0.14m = 1.054m），造成近 40% 的正常回球直接莫名「掛網」。
- **彈道幾何與過網高度雙重防護**：
  - **廚房落點安全區重配**：落點穩定分佈在 `1.15m ~ 1.85m`（`aiShot.z = 1.15 + Math.random() * 0.70`），既確保球穩穩落在 2.13m 廚房線以內，又為過網飛行保留了充裕的拋物線距離。
  - **短球動態過網餘裕補償 (`reqClear`)**：在 `solveArc` 解算器中，當偵測到對手自後場送出網前短球/廚房吊球（`fz < 0 && tz < 2.6`）時，強制將過網預留高度提高 `+0.22m`（`reqClear = NET_CLEAR + 0.22m`），抵消空氣阻力帶來的軌跡下沉。
  - **實測 100 次連續吊球模擬驗證**：連續解算並模擬 100 次第三關廚房吊球，過網淨空全數達到 `14.5cm ~ 45.4cm`，**掛網率降至 0%**，落地 100% 精準位於中興湖廚房區內，完美支持玩家練習「等球彈起後輕推過網」的合法 Dink 動作！

---

### [v5.0.13] - 2026-09-10
**目標檔案**：`匹克球V5.0.13.html`（基於 `v5.0.12` 複製並另存新檔，嚴格遵守親自使用編輯器修復代碼、絕不使用 Python 重寫覆蓋之規範）

#### 1. 🎛️ 經典頂部選單列回歸與現代化抽屜拔除 (Classic Top Nav Restored)
- **問題根因剖析**：
  - 在 v5.0.6 ~ v5.0.7 中強行注入了 `#nav { display: none !important; }`，並改用頂部三膠囊（`#top-actions`）呼叫底部抽屜（`#main-menu-overlay`），破壞了玩家早已熟悉且順手的經典頂欄橫向導覽架構（`📖 規則`、`🔽 設定`、`🔽 關卡`、`🔽 AI體感`、`🏆 榜單`、`🏳️ 棄賽算分`）。
- **還原與架構優化**：
  - 完全移除 `#nav { display: none !important; }` 限制，恢復 `#nav` 與其子抽屜 `#nav-sheet` 的正統浮層佈局與事件綁定。
  - 徹底清除頂欄三膠囊（`#top-actions`）與底部抽屜（`#main-menu-overlay`）的 HTML、CSS 與殘留代碼。
  - 完善面板摺疊動畫與層級控制（`z-index: 35~40`），設定多層子分類（遊戲/體感/檔案/孿生）100% 順暢切換。

#### 2. 🏸 側旋球與動作示範框新增「✕ 關閉視窗」按鈕 (Demonstration Modal Explicit Close Controls)
- **問題根因剖析**：
  - 側旋揮拍指引視窗（`#finger-tutorial`）與示範卡片在過往僅依賴空白處點擊關閉，且在特定狀態下曾因 `pointer-events: none` 導致無法精準點擊關閉，讓使用者感到「視窗被卡住無法關閉」。
- **雙重關閉按鈕設計**：
  - 在側旋手指手勢卡片右上角新增圓形高對比 `✕` 按鈕（`.finger-tut-close-icon`）。
  - 在卡片底部新增醒目操作主按鈕 `[ ✕ 關閉視窗 (開始揮拍) ]`（`.finger-tut-close-btn`），搭配翡翠天藍漸層光效與舒適點擊面積。
  - 於 `.finger-tut-overlay` 樣式中確立 `pointer-events: auto !important;`（僅在隱藏 `.hidden` 時為 `none`），點擊按鈕或卡片外背景皆能 100% 立即關閉視窗。
  - 同步將 `#demo` 教學卡片的跳過按鈕升級為高對比 `[ ✕ 結束示範 ]`，提供使用者全方位的清晰操作控制權。

#### 3. 📱 手機端橫向與直向全局等比放大 (Mobile Landscape & Portrait Scaling Fix)
- **問題根因剖析**：
  - 現代智慧型手機（如 iPhone 13/14/15/16、三星 Galaxy、Pixel 等）在橫向握持時，CSS 像素寬度通常介於 `844px ~ 932px`，但高度僅 `390px ~ 430px`。
  - 過去系統的行動端響應式查詢僅寫 `@media (max-width: 768px)`，因此**手機橫向遊玩時完全不會觸發手機樣式**，反而強制吃到了桌面端 9.5px~11px 的超小字體與極窄內距，使得關卡框、選單列、測速儀與按鍵框在手機高 PPI 螢幕上縮成極小的一坨（「所有框框都變超小」）。
- **直向 + 橫向全方位響應式適配**：
  - 將行動端觸發標準升級為：`@media (max-width: 920px), (max-height: 520px) and (orientation: landscape), (pointer: coarse)`，無縫覆蓋市場上所有主流手機的直向與橫向模式。
  - **關卡資訊框 (`#info`)**：內距增至 `12px 15px`，標題字體加大至 `17px`，難度按鈕與說明文字全面放大至 `11.5px ~ 13.5px`。
  - **頂部選單列 (`#nav`)**：按鈕加大至 `12.5px`，具備 `7px 12px` 的人體工學觸控熱區與防誤觸圓角。
  - **橫向專屬佈局微調**：在手機橫向時，`#info` 固定置於左上角（寬度 250px），`#nav` 自動靠右上角排布（寬度不超過 `calc(100vw - 275px)`），兩者完美並列互不擠壓遮擋，徹底告別微縮框框！
#### 4. 🎨 堅決還原原版高質感懸浮版面美學 (Classic Floating Corner Layout Restored)
- **問題根因剖析**：
  - 在先前嘗試修復直向排版時，曾短暫將 `#nav` 與 `#info` 改為兩條貫穿全寬（`left: 10px; right: 10px;`）的滿版黑條，上下堆疊遮擋了球場上方將近 40% 的空間，嚴重破壞了原版（v5.0.0）廣受喜愛的精緻懸浮卡片質感（「版面配置上沒有我一開始的好看」）。
  - 此外，在電腦瀏覽器的開發者工具（DevTools）模擬 iPhone Pro Max（440x956）時，Chrome 為了在筆電螢幕內塞下 956px 的高度，預設會自動開啟「Fit to window」，強制套用 **`61%`** 縮放！在 61% 縮放下，所有畫面元素與文字會被硬生生縮小將近 40%，造成「重開了還是很小」的錯覺。
- **還原正統懸浮佈局，徹底告別滿版黑條**：
  - **左上角懸浮卡片 (`#info`)**：回歸獨立左上角（`top: 12px; left: 12px; width: min(52vw, 230px);`），保留圓角磨砂玻璃擬態與摺疊能力，中間留白展現 3D 開闊視野。
  - **右上角懸浮膠囊列 (`#nav`)**：回歸獨立右上角（`top: 12px; right: 12px; max-width: calc(48vw - 18px);`），按鈕字體與邊距清晰大器，超出寬度時可滑動，絕不霸道佔滿整行。
  - **底部置中卡片 (`#bottom`)**：平滑懸浮於底端中央，與左右兩側的虛擬搖桿與速度膠囊保持舒適邊界。
#### 5. 📐 全局大比例舒適放大 ╳ 即時版面風格切換預覽 (Large Scale Typography & Live Layout Preview)
- **問題根因剖析**：
  - 玩家明確回饋：「文字框框在實際使用上真的很小，版面配置上也是輸給我第一版的那種」。過去為了遷就極端手機尺寸，過度壓縮卡片寬度（如 230px）與文字（10~12px），導致實際遊玩時必須瞇眼才能看清任務目標與按鍵指引。
  - 同時，第一版（V4）最經典的「左上關卡 · 中央選單 · 右上比分板」平衡對稱美學在 V5 中被移除右上比分板，造成上方視覺不均勻。
- **核心升級實施 (全面落實 A1 預覽 + A2 大比例舒適放大)**：
  1. **預設啟用 V4 經典對稱三欄版面**：
     - **左上關卡資訊卡 (`#info`)**：寬度舒展至 320px，標題放大至 `21px`，說明內文放大至 `15px`（行高 1.55），任務進度 `15.5px`，難度按鈕高 36px，字體清晰大方好讀！
     - **頂部中央選單列 (`#nav`)**：回歸頂部中央置中對稱（`left: 50%; transform: translateX(-50%);`），按鈕字體放大至 `14px bold`，最小觸控高度達 `42px`，大拇指盲點零誤觸。
     - **右上即時雙人比分板 (`#board`)**：經典重現！左側「🪿 玩家」右側「匹克鵝」，搭配 `32px` 粗體發光數字（黃綠光 / 警示紅光），在激戰揮拍時瞄一眼便能掌握比分動態！
  2. **專屬即時版面切換預覽按鈕 (`[ 📐 版面預覽 ]`)**：
     - 於選單列及設定選單常駐按鈕，點擊即可在 **0.1 秒內即時切換**「V4 經典三欄」與「V5 雙角簡約」兩種佈局風格，滿足玩家隨時預覽與挑選最喜愛版面的需求。
  3. **全局各級彈窗與手指示範大比例放大**：
     - 彈窗主體（`.modal-card`）寬度擴展至 `min(94vw, 520px)`，標題 `24px`，說明文字 `15px`，輸入框高度 `46px`，操作大按鈕高度 `48px`。
     - 底部測速與按鍵欄（`#bottom`）：速度數據 `22px`，控制說明 `14px`。
     - 側旋手指教學卡片（`#finger-tutorial`）：標題 `20px`，內文 `15px`，關閉視窗按鈕高度 `48px`。

#### 6. 📱 模擬手機版（iPhone Pro Max）「超小」現象根因剖析與終極滿版近景相機優化 (Mobile Portrait 3D Camera & Zero-Overlap Layout)
- **問題根因徹底剖析**：
  1. **Safari 響應式模式視窗縮放機制（61% 縮放）**：
     - 在使用者上傳的 Safari 截圖中，頂部明確顯示：`iPhone Pro Max 尺寸 440 x 956 ( 61% ) 3倍`。
     - 由於 MacBook 螢幕扣除 Dock、標籤列與網址列後的有效內容高度通常僅約 600px ~ 700px，Safari 為了在筆電螢幕中完整放下高達 956px 的 iPhone 16 Pro Max，會強制將整體畫面等比例縮小為 **61%**！
     - 在 61% 縮放之下，模擬手機在物理螢幕上**實際寬度僅有 268 像素**，原本 13px 的字體在筆電上只剩 7.9px，造成「為什麼模擬手機版還是超小」的視覺落差。
     - **真實體驗解法**：在 Safari 頂部工具列點擊旋轉圖示 `[ ⤢ / 🔄 ]` 切換為 **橫向模式（Landscape 956 x 440）**，因 440px 高度遠小於筆電視窗，Safari 會瞬間以 **`100% 全尺寸 1:1`** 滿版呈現，球場與 UI 豁然開朗！
  2. **舊版本直向相機未拉近，球場淪為中央細條**：
     - 過往 3D 相機位置固定在遠處（$z=13.6\text{m}, y=8.2\text{m}$），在直向比例（$440/956 \approx 0.46$）下，橫向視野大幅縮減至 24°，導致 6.1m 寬的球場僅佔螢幕中央 40%，左右兩側留下大片灰色虛擬空間。
- **終極優化實施方案**：
  1. **直向近景超廣角相機 (`getResponsiveCameraConfig`)**：
     - 將直向模式相機大幅向前平移拉近至 $z=8.4\text{m}, y=4.2\text{m}$（相較桌機模式前推超過 5.2 公尺！），相機目標點鎖定於 $(0, 0.88, -1.0)$。
     - 動態計算垂直 FOV，維持目標橫向視野角度（HFOV = 52°），使 6.1m 球場底線**橫向覆蓋螢幕達 88% 以上**，左右僅保留精緻緩衝邊距，不再有大片灰邊空洞！
     - 史提夫角色位於畫面近端下半部，模型清晰飽滿；匹克球放大至 **1.55 倍**，發光光暈擴大至 11 倍，直向追球一目了然！
  2. **遊戲渲染主循環每幀動態同步相機投射矩陣**：
     - 在 `ren.render` 主循環中，即時偵測 `cam.fov` 與 `cam.aspect`，只要視窗尺寸或旋轉角度變更，0.01 秒內自動呼叫 `cam.updateProjectionMatrix()`，徹底消除視角縮放延遲與形變。
  3. **手機直向零重疊雙層 UI 排版 (Zero-Overlap Responsive Layout)**：
     - **第一層**：左上關卡卡片（寬度 `calc(100vw - 165px)`，標題 18px bold）與右上即時比分板（`26px` 粗體數字）並排，高度嚴格齊平，互不碰撞。
     - **第二層**：中央懸浮橫向膠囊選單列（`top: 180px`），按鈕高度達 `40px`，字體 `13.5px bold`，支援左右平滑滑動；當點擊左上卡片收折時，選單列平滑上升至頂部（`top: 56px`）。
     - 底部測速與按鍵欄加大至 44px 高度與 22px 速度大數字，徹底實現大字體、高對比、不遮擋的極致體感體驗！

#### 7. 👆 手機與觸控裝置卡片自主滑動縮放調整機制 (Interactive Card Resize & Scale Controller)
- **問題根因剖析**：
  - 由於各品牌手機螢幕尺寸、PPI 密度與個人視力距離偏好各有不同，由系統統一寫死單一尺寸難以滿足所有使用者的主觀視覺舒適度。
  - 使用者明確提出：「左上角跟右上角的框框在手機裝置上要讓他可以自己滑動來調整大小」，期望獲得 100% 自主掌控卡片大小的自由度。
- **技術架構與功能實現**：
  1. **GPU 級硬體加速縮放架構 (`transform: scale`)**：
     - 在 `:root` 中定義 `--info-scale: 1.0` 與 `--board-scale: 1.0` 響應式 CSS 變數。
     - `#info` 綁定 `transform: scale(var(--info-scale, 1.0))` 且以 `top left` 為固定原點。
     - `#board` 綁定 `transform: scale(var(--board-scale, 1.0))` 且以 `top right` 為固定原點。
     - 透過 GPU 著色器硬體加速等比縮放，內部所有字體、按鈕、圖示與邊框即時等比放大縮小，**零 DOM 重排 (Zero Reflow)**，保持極致 60 FPS 順暢體驗。
  2. **角落拖曳手柄滑動調節 (`.card-resize-handle`)**：
     - 在 `#info` 右下角放置 `⤡` 手柄，在 `#board` 左下角放置 `⤢` 手柄。
     - 單指觸控或按住滑鼠向外滑動 $\rightarrow$ 卡片平滑放大（最高達 180%）；向內滑動 $\rightarrow$ 卡片平滑縮小（最低至 50%）。
     - 滑動期間於手指上方即時浮現氣泡指示當前百分比（如 `🔍 125%`）。
     - 雙擊手柄即可快速還原至 100% 預設標準尺寸。
  3. **雙指捏合縮放 (Pinch-to-zoom)**：
     - 支援手機觸控螢幕上直接以兩根手指在卡片上捏合（縮小）或分開（放大），直覺符合原生手勢體驗。
  4. **設定抽屜內的精準調節滑桿 (Settings Sliders)**：
     - 於「設定 > 遊戲與視角」抽屜中常駐兩組精細滑動拉桿（`左上關卡` 與 `右上比分`），支援 50% ~ 180% 精確調節，並附有 `[ 🔄 還原卡片預設大小 ]` 按鈕。
   5. **本機持久化記憶 (LocalStorage Persistence)**：
      - 使用者滑動設定的個人專屬卡片比例，自動儲存至 `localStorage`（`nchu_info_scale`、`nchu_board_scale`），重新整理網頁或下次開啟時自動完整保持！

---

### [v5.0.14] - 2026-09-11
**目標檔案**：`匹克球V5.0.14.html`、`匹克球V5.0.14-single.html`、`css/*`、`js/*`、`scripts/build-single.js`

#### 1. 🏗️ 專案架構現代化模組化重構 (Modular Architecture Refactoring)
- **問題根因剖析**：
  - 原先專案代碼過度集中膨脹至 **9,225 行** 單一 HTML，維護難度極高，編輯時極易發生行號漂移或改 A 壞 B。
- **解耦分層架構實施**：
  - **HTML 主入口 (`v14.html` / `匹克球V5.0.14.html`)**：精簡至 1,100 行，保留純淨語意化 DOM。
  - **樣式表模組 (`css/`)**：拆分為 `style.css`（基礎與排版）、`hud.css`（賽場儀表與比分板）、`nav.css`（活動式選單與懸浮球）、`modals.css`（規則手冊與流體力學卡片）。
  - **JavaScript 核心模組 (`js/`)**：拆分為 `config.js`、`audio.js`、`physics.js`、`referee.js`、`motion.js`、`ui.js`、`social.js`、`game.js`。
  - **★ 裁判系統專屬獨立 (`js/referee.js`)**：完全抽離裁判邏輯，統整 4 種即時轉播風格狀態機、四大判罰引擎（廚房區截擊違例、雙彈跳違例、出界觸網、下手發球合規性）與 Side-out 換邊換發計分器。
  - **1 秒單檔自動打包工具 (`scripts/build-single.js`)**：提供 Node.js 打包腳本，隨時可一鍵將模組化檔案自動合成單一獨立 HTML（`v14-single.html`），兼具模組化維護與單檔交付之雙重彈性。

#### 2. 🌪️ 雙軌物理引擎模式與即時切換開關 (Dual Physics Modes: Fast vs. Academic Simulation)
- **問題根因剖析**：
  - 使用者敏銳指出：「手冊卡片列出的馬格努斯效應與流體力學各項參數（$m=0.026\,\text{kg}, \rho=1.225, C_d=0.58, A=0.0043$），原本並未真正運用在微分運算中，而是寫死經驗參數；但若全面強制執行即時微分，終端使用者的手機算力又難以負擔，導致幀率大幅下滑與發燙」。
- **雙軌物理引擎架構設計**：
  1. **模式一：⚡ 極速經驗模式 (Arcade Mode - 預設推薦)**：
     - 採用線性化阻尼因數 `vel.xz *= (1 - 0.12 * h)` 與自旋夾制，零多餘浮點負載，確保任何低階手機皆能跑滿 **60 FPS** 且長效省電。
  2. **模式二：🔬 嚴格學術求解 (Simulation Mode - 專題答辯專用)**：
     - 真實導入 USA Pickleball 官方多孔球空氣動力常數：
       $$\gamma = \frac{\frac{1}{2} C_d \rho A}{m} \approx 0.0587\,\text{m}^{-1}, \quad K_M = \frac{\frac{1}{2} \rho A}{m} \approx 0.101\,\text{m}^{-1}$$
     - **真實速度二次方空氣阻力微分**：$\vec{a}_{\text{drag}} = -\gamma |\vec{v}| \vec{v}$，三軸全面阻尼，出拍初速高速破空減速、過網溫和緩速。
     - **真實馬格努斯外積加速度微分**：$a_{\text{magnus}} \propto K_M \cdot \text{spin} \cdot |\vec{v}_z|$，將側向加速度真正作用於速度導數 $\frac{d\vec{v}_x}{dt}$，達成速度與自旋連動的真實拋物偏折。
- **一鍵即時切換開關**：
  - 於活動式選單（`#nav-subbar`）與規則手冊科普 Card 04 均增設切換開關（`togglePhysicsMode()`），支援一鍵即時切換與 `localStorage` 本機記憶。

#### 3. ⚡ 全局幀率與流暢度效能急救優化 (Performance & 60 FPS Restoration)
- **消除主迴圈 Layout Reflow**：快取 DOM 節點，將速度儀表文字更新頻率降為每 4 幀一次；移除 `loop()` 內的視窗尺寸反覆輪詢與強制重繪。
- **修復球體縮放衝突**：消除 `Physics.sync()` 與相機適配邏輯對 `ball.scale` 的反覆修改，避免 Three.js 每幀重複標記矩陣 Dirty。
- **手機端渲染負載優化**：手機端 `pixelRatio` 上限優化為 `1.2`，關閉高耗能軟陰影，杜絕手機過熱降頻。
- **物理子步長調整**：由 240Hz 微調至 120Hz（`H = 1/120`），維持亞毫米級碰撞精度的同時降低 50% 物理運算耗時，終結延遲惡性循環。

> [!NOTE]
> **⚠️ 開發者測試追蹤注記 (Developer Test Note)**：  
> 本次導入之「雙軌物理模式」與「學術微分參數」目前為**第一版測試驗證版本**。後續將依據使用者實際手感測試反饋、各型號手機續航表現以及專題答辯教授之要求，保留再次微調物理增益係數與公式之調整空間。

#### 4. 🌪️ 觸控滑動側旋香蕉弧線與飛行流光軌跡校準 (Swipe Banana Curve & Luminous Trajectory Calibration)
- **問題根因剖析 (Root Cause)**：
  - 使用者回饋「左右滑球還是沒辦法有軌跡的飛出去」：
    1. **發球完全不採納滑動**：`serveTarget` 僅參考 `padX`，發球瞄準直接忽略了 `SWIPE.peakVx`；且 `doServe()` 之側刷條件 `brushVx > 280 && brushVx > peakVy * 0.45` 因向上發球垂直推力極高而在手機上 99% 無法達成，導致發球側旋強制歸零。
    2. **回擊切球門檻苛刻且數值落入死區**：`tryHit()` 原設定 `brushVx > 150` 且減去 150，使一般側滑只能產生 0.05~0.10 自旋，被 `physics.js` 中的 0.08 死區完全濾除。
    3. **馬格努斯側向偏折視覺量不足**：學術模式原公式側向加速度過微弱（15 公尺航程橫向偏折僅 4~7 公分），在第三人稱遠距視角下肉眼難辨；極速模式亦僅 15~18 公分。
    4. **尾跡粒子數不足且半衰期過短**：舊版粒子僅 10 顆且 0.08 秒內即淡出，無法在空中形成連續的弧線軌跡光帶。
- **全方位校準與實作重點**：
  1. **發球與回擊觸控滑動向量連動**：
     - `serveTarget(p)` 引入 `swipeSteer = clamp((SWIPE.peakVx / 600) * 0.70, -0.80, 0.80)`，使發球能隨滑動方向自然偏向發球區內外角。
     - 發球與回擊側旋靈敏度重整：直推（`|brushVx| < 65~70`）維持 100% 筆直直球；刻意側刷（`>= 65~70`）平滑線性增益至 `0.30 ~ 0.88` 自旋。
     - 支援純水平快速刷球（`swipeHorizPower`），防止橫向滑動時初速不足。
  2. **雙模式馬格努斯空中弧線偏折視覺校準 (0.9m ~ 1.35m)**：
     - **學術模式**：保留嚴謹微分外積微分方程 $\vec{a}_{\text{magnus}} \propto K_M \cdot (\vec{\omega} \times \vec{v})$，將視覺增益放大至 11.5，使側刷時在空中劃出美麗的 0.9m ~ 1.35m 空中香蕉弧線，並由 `softGuard` 保護收斂在場內。
     - **極速模式**：校準經驗加速度 `magnusAcc = (spin * 7.5 + sign(spin) * spin² * 3.8) * factor`，滑移速度放寬至 `[-3.8, 3.8]`，呈現同樣俐落暢快的香蕉弧線。
  3. **炫彩飛行軌跡流光特效 (Luminous Trajectory Trail)**：
     - 擴增 `ballTrail` 精靈至 **22 顆**，延長滯空光影航跡至 0.4 秒。
     - **右側旋香蕉球**：拖曳璀璨霓虹紫流光（`#c084fc`），不透明度增強至 0.48。
     - **左側旋香蕉球**：拖曳電光青藍流光（`#38bdf8`），不透明度增強至 0.48。
     - **直球**：維持經典螢光綠流光（`#dcff6a`）。
  4. **裁判系統與新手示範聯動**：
     - 觸發側旋香蕉弧線時，裁判即時廣播 `🌪️ 右側旋香蕉弧線 (RIGHT CURVE)` / `🌪️ 左側旋香蕉弧線 (LEFT CURVE)`。
     - 互動示範模式 Step 5 側切發球動態注入 0.65 側旋，示範動畫同步呈現精確的空中香蕉拐彎！

#### 5. 🎯 體感操作徹底簡化與直覺化 (Intuitive Arcade Curve & Pure Swipe Control)
- **使用者核心痛點反饋**：
  「沒有啊，他現在球還是隨便亂漂啊，正常來說我覺得應該就簡單化就是讓他們有體驗到瑪格魯斯效應就可以了，我的手指頭往左邊滑球會往左邊飛往右邊邊滑會往右邊飛滑太多會噴出去不用用真的比照真實的運算那個太多了」
- **徹底拔除隨意亂漂根因**：
  1. **移除 `contactSteer` 雜訊**：舊版在擊球時把球拍位置 `padX` 與球點偏移 `(b.x - p.x)` 混入擊球落點，導致玩家滑動過程中球拍跳動造成落點隨意亂漂。現已完全移除，擊球方向 100% 由玩家滑動決定。
  2. **移除 `softGuard` 與雙重加速度干擾**：舊版同時存在 `spinInc`、`vel.x` 與 `softGuard` 邊界拉扯，導致球在邊界附近產生怪異抖動。現已全面收斂至單一 `vel.x` 偏折，零雜訊干擾。
- **純粹直覺的滑動手感**：
  1. **手指往右滑 $\implies$ 球往右飛，空中劃向右側！**
  2. **手指往左滑 $\implies$ 球往左飛，空中劃向左側！**
  3. **手指直推 $\implies$ 100% 筆直向前，零偏漂！**
  4. **滑太多 $\implies$ 自旋過大直接噴出界外（Out of bounds），提供真實的操作失誤與技術回饋！**
